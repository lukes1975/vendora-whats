
# External integrations removed: WhatsApp, Paystack, Google Maps
# If you re-enable these features later, add the necessary keys here.
# Firebase (Vite) environment variables
# Replace the values below with the ones from Firebase Console -> Project settings -> Your apps
# Save these locally to a file named `.env` at the project root (do NOT commit `.env`).
VITE_FIREBASE_API_KEY="AIzaSyBDKGv0hKDZz_HnfndU07qW8Qe2zcApO-8",
VITE_FIREBASE_AUTH_DOMAIN="vendora-f466c.firebaseapp.com",
VITE_FIREBASE_PROJECT_ID="vendora-f466c",
VITE_FIREBASE_STORAGE_BUCKET="vendora-f466c.firebasestorage.app",
VITE_FIREBASE_MESSAGING_SENDER_ID="60983388157",
VITE_FIREBASE_APP_ID="1:60983388157:web:5f98841c5c2c0feac31b94",
VITE_FIREBASE_MEASUREMENT_ID="G-7154Y4G4NC"

# Optional: enable local emulator connections (true/false)
VITE_USE_FIREBASE_EMULATORS=false
# If you later enable emulators set VITE_USE_FIREBASE_EMULATORS=true and configure the host/ports below.
VITE_FIREBASE_AUTH_EMULATOR_URL=http://localhost:9099
VITE_FIREBASE_FIRESTORE_EMULATOR_HOST=localhost
VITE_FIREBASE_FIRESTORE_EMULATOR_PORT=8080
VITE_FIREBASE_STORAGE_EMULATOR_HOST=localhost
VITE_FIREBASE_STORAGE_EMULATOR_PORT=9199

# Notes:
# 1) Create a Firebase project in the Firebase console: https://console.firebase.google.com/
# 2) Enable Authentication -> Sign-in method -> Email/Password
# 3) Add your development origin to Authentication -> Authorized domains (e.g., localhost)
# 4) If using emulators, install Firebase CLI and run `firebase emulators:start` locally
# 5) Never commit real secrets to source control; use CI/hosting environment variables for production.
